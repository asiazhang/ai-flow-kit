<!-- ============================================================
  Changelog 条目模板 — Keep a Changelog 规范
  https://keepachangelog.com/zh-CN/

  使用方法：
  1. 复制本模板中 "## [VERSION]" 以下的内容到 CHANGELOG.md 顶部
  2. 将 <VERSION> 替换为新版本号，<YYYY-MM-DD> 替换为发布日期
  3. 删除不需要的分类小节（空分类不要保留）
  4. 填写各分类下的变更条目（仅记录对使用者可见的变更，内部重构等不写入）
  5. 删除本注释块及模板中的分类说明注释
  ============================================================ -->

## [<VERSION>] - <YYYY-MM-DD>

<!-- 分类说明（发布前删除本注释块）：
  - Added:      新功能、新命令、新 Agent/Skill、新增字段、新增文档
  - Changed:    行为变更、参数调整、重构重命名、性能优化、架构改进
  - Deprecated: 即将移除的功能（标注替代方案和预计移除版本）
  - Removed:    已移除的功能（标注从哪个版本开始 deprecated）
  - Fixed:      Bug 修复
  - Security:   安全漏洞修复

  条目筛选原则：
  Changelog 只记录对使用者可见的变更（命令接口、Agent 行为、Skill 协议、
  配置文件结构等）。内部重构、测试调整、私有函数重命名等不影响使用者的变动不写入 Changelog。

  判断 Added vs Changed 的关键：
  "新增了之前不存在的东西" → Added
  "修改了已有行为/重命名/优化" → Changed
-->

### Added

- **<命令/Agent/Skill>**: <变更描述>

### Changed

- **<命令/Agent/Skill>**: <变更描述>

### Deprecated

- **<命令/Agent/Skill>**: <变更描述>（将在 <VERSION> 移除，请改用 <替代方案>）

### Removed

- **<命令/Agent/Skill>**: <变更描述>（自 <VERSION> 起 deprecated）

### Fixed

- **<命令/Agent/Skill>**: <变更描述>

### Security

- **<命令/Agent/Skill>**: <变更描述>
